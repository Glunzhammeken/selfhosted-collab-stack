---
# Bilag H: Authentik LDAP-outpost
# Kørsel: ansible-playbook site-ldap.yml
# Første gang: playbooken pauser og guider dig til at gemme API-token på serveren
- name: Konfigurér Authentik LDAP-outpost
  hosts: server
  become: true
  roles:
    - authentik_ldap
