---
# Bilag H: Authentik LDAP-outpost
# Kræver: authentik_api_token i group_vars/all/vault.yml (ansible-vault)
# Kørsel: ansible-playbook site-ldap.yml --ask-vault-pass
- name: Konfigurér Authentik LDAP-outpost
  hosts: server
  become: true
  roles:
    - authentik_ldap
