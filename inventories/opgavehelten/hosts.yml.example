# Skabelon for en kundes inventory. Kopiér til hosts.yml og udfyld.
# hosts.yml er gitignored, så de rigtige værdier lækker ikke til repoet.
all:
  hosts:
    server:
      ansible_host: SERVER_IP_HER
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/DIN_NOEGLE_HER