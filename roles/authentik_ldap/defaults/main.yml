---
authentik_dir: "/opt/authentik"
authentik_url: "https://auth.{{ nginx_domain }}"
authentik_domain: "auth.{{ nginx_domain }}"
authentik_ldap_base_dn: "dc={{ nginx_domain.split('.') | join(',dc=') }}"
authentik_ldap_port: 3389
authentik_outpost_name: "ldap-outpost"
authentik_outpost_container: "ak-outpost-ldap-outpost"
# Kendt password til testuser1 og testuser2 — kun til udvikling/test.
# Bruges af Mailcow- og Nextcloud-LDAP-integrationsbilag til at verificere login.
authentik_testuser_password: "Andr3k012!"
